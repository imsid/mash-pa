"""The digest agents.

Two interactive agents — `digest_curator` (the primary) and `digest_concierge`
(interest management) — and two workflow-only agents — `interview_user` and
`run_digest` — that back the `interview-user` and `run-digest` workflows. All four
repurpose the shared SKILLs and digest store in the parent package.
"""
