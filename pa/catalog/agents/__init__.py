"""Personal agents — the product-brief archetypes.

`finance_watch` is a personal agent over your own local data: it registers
unconditionally so the catalog is always browsable, and its capabilities light up
when configuration (data) exists. (The GitHub world now ships as the
`github-digest` workflow under `digest/`, not as a standalone agent here.)
"""
